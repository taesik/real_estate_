import React from "react";
import { TAddPostFormProps } from "./AddPostForm.types";
import {useDispatch} from 'react-redux';
import {nanoid} from '@reduxjs/toolkit';


const AddPostForm: React.FC<TAddPostFormProps> = (props: TAddPostFormProps): React.ReactElement => {
    return <></>;
}

export default AddPostForm;
